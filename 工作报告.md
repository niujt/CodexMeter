# Codex Health 当前工作报告

更新时间：2026-07-29  
范围：免费版本地 macOS 用量监控工具

## 当前状态

免费版的核心闭环已可使用：从本机 Codex 记录读取数据，提供菜单栏摘要、详情仪表盘、桌面小组件、项目与模型分析、趋势、预测、提醒和历史记录。数据仅在本机处理。

本轮已重新构建并启动项目内的 Debug 版（`dist/Codex Health.app`）。最新本机额度记录为“已用 1% / 剩余 99%”。当前尚有未提交的源代码与本报告改动。

## 已完成

### 数据读取与可靠性

- 读取本机 Codex 会话 JSONL，汇总今日、近 7 天、本月与累计 Token。
- 统计输入、输出、缓存输入、推理输出和总 Token。
- 使用 macOS 安全书签保存用户选择的数据目录，重启后仍可访问。
- 采用最新的 7 天额度事件，不再固化使用旧的额度标识；当前本机记录已同步为已用 1%、剩余 99%。
- 支持手动刷新和 30 秒、1 分钟、5 分钟自动刷新。

### 菜单栏与桌面小组件

- 菜单栏显示当前 7 天额度剩余比例。
- 剩余比例颜色：50% 及以上绿色，20%–49% 黄色，低于 20% 红色。
- 下拉面板提供额度、重置时间、预测、低额度提醒阈值、同日提醒去重、打开详情和退出操作。
- WidgetKit 小组件：小号展示额度概览；中号展示今日 / 近 7 天 / 本月 Token、7 天额度与上下文。
- 应用与小组件通过 App Group 共享本地摘要，支持刷新后同步更新。

### 详情仪表盘

- 健康报告：当前额度、健康状态、预计可持续使用时长、上下文和基础风险提示。
- 使用趋势：按天与近 24 小时查看 Token 趋势。
- 模型与效率：模型 Token 占比、请求次数和端到端平均轮次耗时。
- 项目与用量：按今天、近 7 天、近 30 天浏览项目用量，支持完整路径显示。
- 预测与风险：按本机额度历史和近 1 / 6 / 24 小时变化加权计算预计耗尽时间。
- 历史记录：按时间范围、模型、项目筛选会话摘要；不展示会话正文。

### 提醒与设置

- 支持低额度提醒阈值配置。
- 支持同日提醒去重。
- 支持额度可能在重置前耗尽、即将重置等本地通知。
- 设置页提供数据目录授权、自动刷新频率、提醒选项和隐私说明。

### 分发与隐私

- 提供 DMG 分发流程和 Gatekeeper 首次打开指引。
- README 已使用脱敏的演示截图。
- Git 仓库规则已排除本地配置、私有工作区和用户数据；公开内容不应包含真实账号、Token、数据目录、项目名称或真实用量截图。

## 本轮进行中的改动（尚未提交）

1. 修正额度读取：始终选择最新 7 天额度窗口，防止旧窗口覆盖当前额度。
2. 历史记录新增“7 天额度周期时间线”，按重置日期合并同一周期内的多次刷新，只保留该周期最新的本机采样，显示：
- 最近采样时间；
- 该时刻已用 / 剩余额度；
- 该周期对应的重置时间。
3. 已通过 Debug 构建、应用签名和启动进程校验；当前启动的是项目内 `dist/Codex Health.app`。

## 已知限制

- 数据来源依赖 Codex 本机 JSONL 格式；若官方记录格式变化，解析逻辑可能需要适配。
- 项目与模型统计以会话记录中的累计 Token 为基础，不能保证等同于官方服务端账单统计。
- 平均耗时为“用户消息至后续助手消息”的端到端轮次耗时，不是服务端 API 延迟。
- 用量预测依赖本机已积累的历史样本；样本较少时准确性有限。
- 额度周期时间线仅展示本机曾观察到的周期采样，不是官方完整历史；未观察到的周期不会被补造。
- 应用目前为本地签名分发，首次打开仍可能需要用户在 macOS“隐私与安全性”中选择“仍要打开”。

## 下一步建议

1. 确认界面展示符合预期后，提交 3 个源文件与本工作报告的改动。
2. 在真实使用一段时间后，检查预测与提醒的准确性并微调默认阈值。
3. 为历史记录增加导出 CSV（仅导出汇总，不导出会话正文）。
4. 为不同 Codex 记录格式加入兼容检测和更明确的错误提示。
5. 在免费版稳定后，再从共享数据层和功能开关拆分商业版能力，避免长期维护两套重复代码。

## 本地验证方式

```bash
cd CodexMeter
./script/build_and_run.sh --verify
```

构建产物位于 `dist/Codex Health.app`。发布或提交前，请再次确认 `git status` 中没有本机配置、私有截图、会话记录或个人路径。
