# Git 提交约定

- 本项目的 Git 提交作者和提交者邮箱一律使用 `670482466@qq.com`。

# 诊断与排查规范

- 默认最小化读取：先限定项目、文件类型与时间范围。
- 默认只读取最近两天的日志或会话记录，禁止扫描整个 Codex 目录或历史记录。
- 开始读取前，先向用户列出拟读取的文件清单和范围；只有用户二次确认后才执行读取。
- 若结果不足，再逐步扩大读取范围，并重新说明新增范围。

# 构建部署规则

- 部署前删除 `/Applications` 中已安装的旧版 `Codex Health`，安装新版并确认可以启动。
- 随后清理项目本地生成的新版安装包或构建产物，但绝不删除项目源码、用户数据或配置。
- 任何删除操作前，必须列出精确目标和影响范围，并获得用户二次确认。
- 发布 DMG 的安装窗口必须同时包含 `Codex Health.app` 和指向 `/Applications` 的快捷方式/别名，支持用户将应用拖拽到该快捷方式完成安装。
